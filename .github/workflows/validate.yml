name: validate

# Content-integrity and test gates. The `integrity` job uses inline bash +
# python3 stdlib only, does NOT execute checked-out repository code (reads
# files only) apart from scripts/check_release.py, and runs on pull_request
# (not pull_request_target) so a fork PR cannot gain write access. The `tests`
# job executes repository code read-only on pull_request with no secrets.

on:
  push:
  pull_request:

permissions: read-all

jobs:
  integrity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Content integrity check
        run: |
          set -euo pipefail
          # Sentinels assembled from parts so this workflow never self-matches.
          P1="CONFIDENTIAL"
          P2="BEGIN $(printf 'PRIVATE')  KEY"
          P3="BEGIN $(printf 'OPENSSH PRIVATE') KEY"
          pat="$P1|BEGIN [A-Z]* ?PRIVATE KEY"
          if grep -RInE "$pat" --exclude-dir=.git --exclude-dir=.github . ; then
            echo "::error::leak sentinel found in shippable content"
            exit 1
          fi
          echo "OK: no leak sentinels."

      - name: SKILL.md frontmatter lint
        run: |
          python3 - <<'PY'
          import re, sys, pathlib
          fail = False
          skills = list(pathlib.Path('.').rglob('SKILL.md'))
          if not skills:
              print('::error::no SKILL.md found'); sys.exit(1)
          for p in skills:
              if 'book_to_skill' in p.parts:  # vendored engine has no SKILL.md anyway
                  continue
              t = p.read_text(encoding='utf-8')
              m = re.match(r'^---\s*\n(.*?)\n---\s*\n', t, re.S)
              if not m:
                  print(f'::error::{p}: missing YAML frontmatter'); fail = True; continue
              fm = m.group(1)
              if not re.search(r'^name:\s*\S', fm, re.M):
                  print(f'::error::{p}: frontmatter missing name'); fail = True
              if not re.search(r'^description:\s*\S', fm, re.M):
                  print(f'::error::{p}: frontmatter missing description'); fail = True
          print('OK: SKILL.md frontmatter valid.' if not fail else 'frontmatter lint failed')
          sys.exit(1 if fail else 0)
          PY

      - name: Version consistency (RR-S-11)
        run: |
          python3 - <<'PY'
          import re, json, sys, pathlib
          def read(p):
              q = pathlib.Path(p)
              return q.read_text(encoding='utf-8') if q.is_file() else None
          def changelog_top():
              t = read('CHANGELOG.md')
              if not t:
                  return None
              for m in re.finditer(r'^##\s*\[?v?(\d+\.\d+\.\d+)', t, re.M):
                  return m.group(1)  # first released semver; skips '## [Unreleased]'
              return None
          def release_info():
              m = re.search(r'^Version:\s*(\d+\.\d+\.\d+)', read('RELEASE-INFO.txt') or '', re.M)
              return m.group(1) if m else None
          def pyproject():
              m = re.search(r'^version\s*=\s*"(\d+\.\d+\.\d+)"', read('pyproject.toml') or '', re.M)
              return m.group(1) if m else None
          def plugin(p):
              t = read(p)
              return json.loads(t).get('version') if t else None
          def citation():
              m = re.search(r'^version:\s*(\d+\.\d+\.\d+)', read('CITATION.cff') or '', re.M)
              return m.group(1) if m else None
          def readme_badge():
              m = re.search(r'version-(\d+\.\d+\.\d+)-informational', read('README.md') or '')
              return m.group(1) if m else None
          def readme_prompt():
              m = re.search(r'jgs-reference-skill[^\n]*\(v(\d+\.\d+\.\d+)\)', read('README.md') or '')
              return m.group(1) if m else None
          def landing():
              t = read('docs/index.html') or ''
              vals = set(re.findall(r'"softwareVersion":\s*"(\d+\.\d+\.\d+)"', t))
              vals |= set(re.findall(r'REV <b>(\d+\.\d+\.\d+)</b>', t))
              return vals.pop() if len(vals) == 1 else (None if not vals else ';'.join(sorted(vals)))
          vals = {'CHANGELOG': changelog_top(), 'RELEASE-INFO': release_info(),
                  'pyproject': pyproject(), 'plugin.json': plugin('.claude-plugin/plugin.json'),
                  'CITATION.cff': citation(), 'README badge': readme_badge(),
                  'README prompt': readme_prompt(), 'landing': landing(),
                  'cursor plugin.json': plugin('.cursor-plugin/plugin.json'),
                  'gemini-extension.json': plugin('gemini-extension.json')}
          print(vals)
          uniq = set(v for v in vals.values() if v)
          if None in vals.values() or len(uniq) != 1:
              print('::error::version mismatch across sources'); sys.exit(1)
          print(f'OK: all versions agree at {uniq.pop()}.')
          PY

      - name: Release gate (RR-B-15)
        run: python3 scripts/check_release.py

  tests:
    runs-on: ubuntu-latest
    permissions: read-all

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package and pytest
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[all]" pytest

      - name: Pytest suite
        run: pytest -q

      - name: Tools self-check
        run: |
          for t in vet_source check_overlap outline validate_pack pack_eval; do
              python3 tools/$t.py --self-check
          done

      - name: Compile smoke
        run: python3 -m py_compile tools/*.py scripts/extract.py
